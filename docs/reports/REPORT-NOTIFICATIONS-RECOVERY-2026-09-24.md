# گزارش موج اعلان‌ها — ۲۰۲۶-۰۹-۲۴

## چه افزوده شد
- خطای بارگذاری اعلان‌ها از فهرست واقعاً خالی جدا شد و دکمهٔ تلاش دوباره دارد.
- شکست علامت‌گذاری به‌عنوان خوانده‌شده، حذف اعلان یا خواندن همه دیگر رابط را به‌اشتباه به‌روزرسانی نمی‌کند و با toast گزارش می‌شود.
- آیتم‌های اعلان با صفحه‌کلید باز می‌شوند و دکمه‌های عملیاتی نام دسترس‌پذیر دارند.

## چه بررسی شد
- `npx tsc --noEmit` موفق بود.
- `git diff --check` موفق بود.
- فایل‌های ترجمه parse شدند و سایت لوکال HTTP 200 داد.

## چه باقی ماند
- refactor جامع همهٔ صفحه‌ها هنوز تمام نشده؛ این موج فقط پنل اعلان‌ها را پوشش می‌دهد.
- تست خودکار، build و بررسی بصری Chrome اجرا نشد.

## گام بعد
- تهیهٔ فهرست پوشش واقعی و ادامهٔ صفحه‌های باقی‌ماندهٔ فرانت.

## واژه‌نامه
- **صفحه‌کلید:** تعامل با Enter/Space به‌جای کلیک.
- **toast:** پیام کوتاه وضعیت عملیات.
